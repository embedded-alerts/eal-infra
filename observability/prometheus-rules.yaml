groups:
  - name: eal.availability
    rules:
      - alert: EALServiceUnavailable
        expr: up{namespace=~"eal.*"} == 0
        for: 5m
        labels: { severity: page }
        annotations:
          summary: "Embedded Alerts service unavailable"
