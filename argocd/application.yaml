apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: eal-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/embedded-alerts/eal-infra.git
    targetRevision: main
    path: k8s/overlays/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: eal-dev
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions: [CreateNamespace=true]
